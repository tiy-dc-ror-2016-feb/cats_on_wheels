<%= form_for([@pet, @visit]) do |f| %>
  <% if @visit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@visit.errors.count, "error") %> prohibited this visit from being saved:</h2>

      <ul>
      <% @visit.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.hidden_field :pet_id %>

  <div class="field">
    <%= f.label :notes %><br>
    <%= f.text_area :notes, class: 'materialize-textarea' %>
  </div>
  <div class="actions">
    <%= f.submit  options = { class: 'waves-effect waves-light btn'}  %>
  </div>
<% end %>
